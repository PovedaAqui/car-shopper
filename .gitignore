node_modules/
.env
.env.*
!.env.example
*.log
dist/
.DS_Store
# Convex-generated (regenerated locally) — committed for hackathon reproducibility
# convex/_generated/
# Local runtime artifacts
.local/
*.local.json
worker/state/

# Local working state report — never commit/push
STATE.md

# Local-to-production execution plan — contains operational notes
LOCAL_TO_PRODUCTION_PLAN.md

# Secrets / credentials — NEVER commit or push
hackathon-keys
.env.local
